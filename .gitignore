# Usually needed for every working project instance
# and must be copied and (if necessary) adapt.
public/.htaccess
public/.htpasswd
/*/simpledbmigration/config.php

# Usually needed for every working project instance
# and must be generated.
composer.lock
/public/sitemap*.xml
/data/sitemap/sitemap*.xml
/*/tablemapping/mappingresult.txt

# Usually not needed for a working project instance.
nbproject
._*
.~lock.*
.buildpath
.DS_Store
.idea
.project
.settings
.idea

# Other (e.g just for info).
config/application.config.php.default
dbdummydata.sql
/module/*/test/report/
