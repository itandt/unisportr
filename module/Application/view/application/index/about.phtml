<?php
$title = $this->translate('Hochschulsport 2.0');
$this->headTitle($title);
?>

<h1 id="pageTitle"><?php echo $this->escapeHtml($title); ?></h1>

<h2 id="pageSubTitleHomeWelcome" class="pageSubTitle">Willkommen bei unisport<em>r</em>!</h2>
<p>unisport<em>r</em> ist die erste Suchmaschine für Hochschulsportkurse an allen größ<em>r</em>en Universitäten in Deutschland.</p>
<p>unisport<em>r</em> ist erst am Anfang. Wenn Ihr Ideen habt od<em>r</em> eine weitere Uni vorschlagen wollt, schreibt uns einfach.</p>
<p>unisport<em>r</em> wird noch viel meh<em>r</em>. Es erwarten euch viele zusätzliche Funktionen rund um den Hochschulsport. Also schaut wied<em>r</em> rein.</p>
<p>Euer unisport<em>r</em> Team</p>
<p>Bisher unterstützte Universitäten:</p>

<ul id="catalogListUniversities" class="catalogList">
<?php foreach ($universities as $university) : ?>
	<li>
		<?php
		$aURL = $this->url('city',  array('city' => $this->escapeHtml($university->cityName)));
		$aText = $this->escapeHtml($university->displayedName);
		?>
		<span><a href="<?php echo $aURL; ?>"><?php echo $aText; ?></a></span>
	</li>
<?php endforeach; ?>
</ul>